#include <bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> st; //syntax 

    //this is used to insert elements into the set.
    st.insert(1);
    st.insert(1);
    st.insert(2);
    st.insert(2);
    st.insert(3);

    //to erase/remove a specific entry: 
    st.erase(3);
   
}